name: github-actions-workflow-test
on: push
jobs:
  hello-world:
    runs-on: ubuntu-latest
    environment: ${{ vars.ENVIRONMENT }}
    steps: 
      - name: echo message
        run: echo "Hello ${{ vars.ENVIRONMENT }}!"
