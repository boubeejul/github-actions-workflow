{
  "name": "lab-cicd-intermediate",
  "version": "1.0.0",
  "scripts": {
    "test:unit": "echo 'Running unit tests...' && exit 0",
    "test:integration": "echo 'Running integration tests...' && exit 0",
    "build": "mkdir -p dist && echo 'Build successful' > dist/index.html"
  }
}